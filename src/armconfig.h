/*
   Configuration for the paint bot's arm
 */

#ifndef ARMCONFIG_H_
#define ARMCONFIG_H_

#include "utils.h"

const Link links[] = {
   //Base base(300, link1),
   //Link link1(PRISMATIC, 150, base, link2),
   //Link link2(REVOLUTE, 100, link1, link3),
   //Link link3(REVOLUTE, 75, link2, brush),
   //Brush brush(link3)
};

#endif

